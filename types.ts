export type ExecutedWork = {
  id: string;
  imageSource: string;
  imageAlt: string;
  title: string;
  description: string;
  year: string;
};


export type FormData = {
  name: string;
  email: string;
  message: string;
};
